{
  "name": "gramic",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm run build",
    "vercel-build": "cd client && npm run build"
  },
  "dependencies": {
    "react-scripts": "^5.0.1"
  }
}
"postinstall": "cd client && npm install"
